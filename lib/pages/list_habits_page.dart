import 'package:flutter/material.dart';

class ListHabitsPage extends StatelessWidget {
  const ListHabitsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}